<div class="navbar">
	
	<%=link_to "Home", root_path %>
	<%=link_to "About us",pages_about_us_path %>
	<!--<%=link_to "Contact",pages_contact_path %> !-->
	<%=link_to "See posts",posts_path %>
	

	<% if session[:user_id] %>
		<%=link_to "Report problem",new_contact_path %>
		<%=link_to "Logout", logout_path %>
		<%=link_to "Edit profile", edit_user_path(session[:user_id]) %> 
	<% else %>
		<%=link_to "Login", new_login_path %>
		<%=link_to "Signup", new_user_path%>
	<% end %>
	
	
</div>